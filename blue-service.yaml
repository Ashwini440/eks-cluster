apiVersion: v1
kind: Service
metadata:
  name: marvel-service-blue
  annotations:
    harness.io/primary-service: "true"
spec:
  selector:
    app: marvel
    version: blue  # Initially pointing to Blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: LoadBalancer
